import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Dr. Serena Blake - Clinical Psychologist | Los Angeles Therapy",
  description:
    "Licensed clinical psychologist in Los Angeles specializing in anxiety, relationship counseling, and trauma recovery. Book your free consultation today.",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
